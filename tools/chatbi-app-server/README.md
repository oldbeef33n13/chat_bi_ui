# chatbi-app-server

P1 backend base for Chat BI templates.

## Run

```bash
mvn -f tools/pom.xml -pl chatbi-app-server spring-boot:run
```

Default port: `18080`

## Test

```bash
mvn -f tools/pom.xml -pl chatbi-app-server test
```
